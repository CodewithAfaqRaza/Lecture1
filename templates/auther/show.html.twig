{% extends 'base.html.twig' %}

{% block title %}Auther{% endblock %}

{% block body %}
    <h1>Auther</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auther.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ auther.name }}</td>
            </tr>
            <tr>
                <th>Fathername</th>
                <td>{{ auther.fathername }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ auther.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_auther_index') }}">back to list</a>

    <a href="{{ path('app_auther_edit', {'id': auther.id}) }}">edit</a>

    {{ include('auther/_delete_form.html.twig') }}
{% endblock %}
